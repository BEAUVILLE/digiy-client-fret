# 📦 DIGIY FRET — Module Client (Taxi-bagages / Marchandises)

Interface client pour commander un transport de marchandises :
ENVELOPE • COLIS • BAGAGE • GROS VOLUME

## 🚀 Démarrage rapide
1. Ouvre `client-fret.html` dans ton navigateur
2. Clique **Estimer le prix**
3. Renseigne téléphone + zones
4. Clique **Commander FRET**

✅ Une course est créée dans `driver_trips` avec `service_type = 'FRET'`.

## 🧮 Pricing (anti-dumping)
- Prix calculé côté serveur via RPC `estimate_fret_price`
- Plancher garanti (chauffeur protégé)
- Options : manutention, étages, fragile

## 🔧 Dépendances
- Supabase (RPC)
- Fonctions requises :
  - `estimate_fret_price(...)`
  - `create_trip(...)`

## 📁 Fichiers
- `client-fret.html` : module client FRET
- `README.md` : documentation
